<h1>All Posts</h1>

<% @posts.each do |post|%>
<div class="card mb-3">
    <div class="card-title m-3">
    <%= link_to post.title, post_path(post.id) %>
        <div class="card-body">
            <%=post.body%>
        </div>
    </div>
</div>
<% end %>